---
title: REST API란?
date: '2022-12-21'
tags: ['API', 'REST API']
draft: false
---

## 1. REST API란?

REST는 `Representational State Transfer`의 약자다. REST는 로이 필딩의 논문에서 최초로 소개되었는데,
당시 웹(HTTP)의 장점을 최대한 활용 할 수 있는 아키텍처로 REST를 발표 했다고 한다.
REST는 클라이언트가 서버에서 데이터를 액세스 하는데 사용할 수 있는 `GET`,`POST`,`PUT`,`DELETE` 네가지 아키텍처를 제공하고 있다.

## 2. REST의 특징

### 1) Uniform(유니폼 인터페이스)

Uniform Interface는 URI로 지정한 리소스에 대한 조작을 통일하여 한정적인 인터페이스로 수행하는 아키텍처 스타일을 말한다.

### 2) Stateless(무상태성)

REST는 무상태성 성격을 갖는다. 무상태성이란 작업의 상태정보를 저장하고 따로 관리하지않고, 단순히 들어오는 요청을 처리한다.
따라서 서비스의 자유도가 높아지고 불필요한 정보를 관리하지 않음으로 구현이 단순해진다.

### 3) Cacheable(캐시 가능)

REST의 가장 큰 특징 중 하나는 HTTP라는 기존 웹표준을 그대로 사용하기 때문에, 웹에서 사용하는 기존 인프라를 그대로 활용 가능하다.
따라서 HTTP가 가진 캐싱 기능 적용이 가능하다.

### 4) Self-descriptiveness(자체 표현 구조)

REST의 큰 특징 중 하나는 REST API의 메시지만 보고도 쉽게 이해 할 수 있는, 자체 표현 구조로 되어 있다는 것이다.

### 5) Client - Server 구조

REST 서버는 API 제공, 클라이언트는 사용자 인증이나 컨텍스트(세션, 로그인 정보) 등을 직접 관리하는 구조로
각각의 역할이 확실히 구분되기 때문에 클라이언트와 서버에서 개발해야 할 내용이 명확해지고 서로의 의존성이 줄어들게 된다.

### 6) 계층형 구조

REST 서버는 다중 계층으로 구성될 수 있으며 보안, 로드 밸런싱, 암호화 계층을 추가해 구조상의 유연성을 둘 수 있고
PROXY, 게이트웨이 같은 네트워크 기반의 중간매체를 사용할 수 있게 한다.

## 3. REST API 디자인 가이드

### 1) URI는 정보의 자원을 표현해야 한다.

- 리소스 명은 동사보다 명사를 활용한다.

```
GET /members
```

### 2) 자원에 대한 행위는 HTTP Method(GET, POST, PUT, DELETE)로 표현한다.

| Method | 설명                                                                              |
| ------ | --------------------------------------------------------------------------------- |
| POST   | URI를 요청하면 리소스를 생성한다.                                                 |
| GET    | 리소스를 조회한다. 리소스를 조회하고 해당 도큐먼트에 대한 자세한 정보를 가져온다. |
| PUT    | 해당 리소스를 수정한다.                                                           |
| DELETE | 해당 리소스를 삭제한다.                                                           |

위와 같은 아키텍처에 따른 API를 `Restful API`이다.

## 4. URI 설계시 주의 할 점

    1) 슬래시 구분자(/)는 계층 관계를 나타내는 데 사용된다
    2) URI 마지막 문자로 슬래시(/)를 포함하지 않는다. (분명한 URI를 위해)

    ```
    https://api.com/cat/name/ (x)
    https://api.com/cat/name (o)
    ```
    3) 불가피하게 긴 URI를 사용하면 하이픈(-)을 사용한다.
    4) 언더바(_)는 가독성을 떨어트리기 때문에 사용하지 않는다.
    5) 소문자가 적합하다. 대소문자에 따라 다른 리소스로 인식한다.
    6) 파일 확장자는 URI에 포함시키지 않는다. (대신 accept header를 사용한다)

## 5. 자원을 표한하는 collection과 document

- document: 문서, 하나의 객체
- collection: 문서들의 집합, 객체들의 집합

아래 예시에서 collection은 `sports`, `players`

document는 `soccer`, `13`이다.

```
   http:// restapi.example.com/sports/soccer/players/13
```

[참고 NHN 개발블로그](https://meetup.toast.com/posts/92)

[참고 aws](https://aws.amazon.com/ko/what-is/api/)
